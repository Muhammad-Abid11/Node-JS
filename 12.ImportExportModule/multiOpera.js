const multiple = (a, b) => a * b;

const sub = (a, b) => a - b;

const name = "vinod"

// module.exports.multiple = multiple;
// module.exports.sub = sub;

//  in another easy way

module.exports = { multiple, sub, name }
